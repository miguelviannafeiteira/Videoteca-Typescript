import React, { createContext } from 'react'

// export const VideoContext = createContext<string | null>(null)

// const VideContextProvider = ({children}) => {
//   return (
//     <VideoContext.Provider>
//     {children}
//     </VideoContext.Provider>

//   )
// }

// export default VideContextProvider
